boto3>=1.34
# The following are Lambda-layer dependencies, not bundled in this deployment
# package — attach a layer providing whichever of these a given
# managed_secrets configuration actually needs (see handler.py docstring):
#   psycopg2-binary   # secret_type = postgres
#   redis             # secret_type = redis
#   kafka-python       # secret_type = kafka_scram
