{
  "name": "@ams/deploy-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Deployment pipeline scripts (WO-010): audit event publishing, smoke tests, contract tests.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx tests/*.test.ts"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.0"
  }
}
