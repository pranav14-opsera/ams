# Documented exceptions to check_sensitive_files (WO-011), one exact
# repo-relative path per line. Empty on purpose — every sensitive-looking
# file this repo tracks today is either a real key (there are none) or
# already excluded via .gitignore before it'd ever reach a commit. Add an
# entry here only with a comment explaining why it's genuinely not a
# secret (e.g. a documented .env.example with placeholder values).
