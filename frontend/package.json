{
  "name": "@ams/frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Next.js scaffold proving the WO-007 build pipeline (Docker + strict TypeScript) works end to end. The full design system, routing, and pages are WO-050's scope.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0"
  }
}
