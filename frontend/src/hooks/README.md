# Hooks

Cross-feature React hooks (e.g. `useMediaQuery`, `useDebouncedValue`) live
here. A hook used by only one feature belongs under that feature's own
directory instead (`src/features/<feature>/hooks`).
