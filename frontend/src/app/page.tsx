import { Button } from "@/components/ui/button";

export default function HomePage() {
  return (
    <>
      <p>
        Turnkey multi-agent management platform — shell scaffold (WO-050) with role-aware sidebar navigation (WO-051) and WCAG 2.1 AA accessibility
        infrastructure (WO-053). Feature pages land in subsequent work orders.
      </p>
      <Button>Get started</Button>
    </>
  );
}
